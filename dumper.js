const fs = require('fs');
const { chromium } = require('playwright');
(async () => {
  console.log('Este script no tiene sentido si requiere certificado y no interactúa, mejor usar test_almudena.js');
})();
